[project]
name = "qsim-py"
version = "1.3.7"
description = "Quantitative simulation stack: qsim core engine + data providers + Prefect-based data tools."
readme = "AGENTS.md"
requires-python = ">=3.12"
license = { text = "MIT" }

# 依赖来源:
#   - tools/release/setup/setup.py (qsim core; 仅做 wheel 打包)
#   - 实际源码 import (numpy/pandas/lz4/pymysql/...)
#
# TODO(deps): 以下依赖待整理 ——
#   * prefect 已升级到 3.x
#   * pycryptodome / redis 仅 build_cython.py 的 copy_only_list 提到 (crypto_api.py / redis_client.py
#     源码目前不在仓库里), 真正需要时再加
dependencies = [
    # qsim core
    "numpy>=1.13.0",
    "pandas",
    "pyyaml",
    "sortedcollections",
    "click",
    "numcodecs",
    "cython",
    "xmltodict",
    "lz4",
    "pymysql",
    # qsim_data_tools
    "prefect>=3.0",
    "paramiko",
    "pytz",
]

[project.optional-dependencies]
dev = [
    "ipython",
    "jupyter",
]

[project.scripts]
qsim = "qsim.qsim_run:main"
stats_general = "qsim.modules.stats_general:main"
update_tools = "qsim_data_tools.update_tools:cli"

# flat layout
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["./qsim", "./qsim_data_tools"]

[tool.uv]
package = true
